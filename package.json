{
  "name": "ERC721-Transfer",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ anilmuthigi/ERC721-Transfer",
    "create-local": "graph create --node http://localhost:8020/ anilmuthigi/ERC721-Transfer",
    "remove-local": "graph remove --node http://localhost:8020/ anilmuthigi/ERC721-Transfer",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 anilmuthigi/ERC721-Transfer"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.26.0",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
